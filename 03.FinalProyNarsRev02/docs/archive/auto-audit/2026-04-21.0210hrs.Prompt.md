Actúa como engineer senior React especializado en autenticación, bootstrap de sesión y refactors de bajo riesgo.

OBJETIVO:
Mejorar el bootstrap de autenticación en `ecommerce-app-Nars/src/contexts/AuthContext.jsx` para que la sesión NO se restaure solo desde `localStorage`, sino que se revalide contra backend al iniciar la app, SIN romper el contrato actual del contexto, guards, tests ni comportamiento observable esperado.

CONTEXTO:
- El proyecto está en fase de cierre para maximizar puntuación de rúbrica.
- La auditoría vigente marcó `II.1 Autenticación` como parcial porque `AuthContext` restaura la sesión desde `localStorage` sin revalidación inicial obligatoria contra backend.
- Ya están verdes:
  - suite frontend
  - refactor de `CartContext` con `useReducer`
  - lazy loading ampliado
  - specs Cypress de auth, orders y cart
- Este cambio debe ser de bajo riesgo y orientado a recuperar el punto faltante de autenticación sin introducir regresiones.

ARCHIVOS OBJETIVO PRINCIPALES:
- `ecommerce-app-Nars/src/contexts/AuthContext.jsx`
- `ecommerce-app-Nars/src/services/authService.js`
- `ecommerce-app-Nars/src/api/authApi.js`

BACKEND DISPONIBLE:
- Existe endpoint real `GET /api/users/me`
- El frontend ya cuenta con `apiClient` e interceptores Axios para adjuntar JWT y manejar refresh ante 401

OBJETIVO FUNCIONAL EXACTO:
- Si hay tokens/sesión persistida al arrancar la app:
  - NO marcar inmediatamente `isAuthenticated=true` solo por `localStorage`
  - revalidar sesión con backend usando `GET /api/users/me` (o flujo equivalente justificado)
  - si backend confirma, hidratar `user` y dejar sesión activa
  - si backend falla, limpiar sesión y dejar usuario deslogueado
- Mantener compatibilidad con:
  - `PrivateRoute`
  - `AdminRoute`
  - `GuestOnlyRoute`

REGLAS CRÍTICAS:
- SÍ debes implementar los cambios
- NO cambiar la API pública del contexto salvo que sea estrictamente aditivo y no rompa consumidores
- NO cambiar nombres de funciones expuestas si no es indispensable
- NO cambiar comportamiento observable de login/logout más allá de la mejora del bootstrap
- NO romper compatibilidad con Vitest ni Cypress
- Aprovechar el `apiClient` y los interceptores existentes; NO duplicar lógica de auth innecesariamente
- Hacer el cambio mínimo necesario
- Mantener clara la separación:
  - `AuthContext` = estado/orquestación
  - `authService` = persistencia/bootstrap helpers
  - `authApi` = llamadas HTTP
- Incluir TODO lo que salga en terminal dentro del documento final
- El documento final debe llevar timestamp obligatorio en formato `YYYY-MM-DD.HHmmhrs`

API PÚBLICA A PRESERVAR / VALIDAR EXPLÍCITAMENTE:
- `user`
- `isAuthenticated`
- `loading`
- `login`
- `logout`
- cualquier otra propiedad/función actualmente expuesta por `AuthContext` debe preservarse si existe

ESTRATEGIA ESPERADA:
1. Analizar el bootstrap actual en `AuthContext.jsx`
2. Identificar cómo se restaura hoy la sesión desde `localStorage`
3. Agregar en `authApi` una llamada explícita a `GET /api/users/me` si aún no existe
4. Agregar en `authService` un helper tipo `bootstrapSession()` o equivalente, reutilizable y claro
5. Modificar `AuthContext` para:
   - iniciar en estado de carga
   - revisar si existe sesión persistida
   - revalidar con backend antes de marcar autenticación restaurada
   - limpiar sesión si la revalidación falla
6. Validar que login/logout sigan funcionando como antes
7. Validar que guards sigan funcionando correctamente
8. Ejecutar pruebas
9. Documentar cambios

VALIDACIONES OBLIGATORIAS:
Ejecutar, como mínimo:
1. `npm test -- src/contexts`
2. `npm test -- src/pages`
3. `npm test`
4. `npx cypress run --spec cypress/e2e/auth.cy.js`
5. `npx cypress run --spec cypress/e2e/authLifecycle.cy.js`
6. Si el cambio impacta guards o navegación protegida, correr también:
   - `npx cypress run --spec cypress/e2e/profile.cy.js`
   - o la spec mínima adicional que justifiques

VALIDACIÓN FUNCIONAL MÍNIMA:
Confirmar explícitamente, sin regresión visible:
- la app no se considera autenticada al bootstrap solo por `userData` local
- login sigue funcionando
- logout sigue funcionando
- sesión válida se restaura correctamente tras recarga
- sesión inválida o revocada se limpia correctamente
- `PrivateRoute`, `AdminRoute` y `GuestOnlyRoute` siguen respetando el estado auth correcto

OUTPUT OBLIGATORIO:
Crear documento en:
- `docs/auto-audit/`

Nombre:
- `YYYY-MM-DD.HHmmhrs.AUTH_BOOTSTRAP_REVALIDATION.md`

ESTRUCTURA DEL DOCUMENTO:
1. Estado inicial
2. Diagnóstico técnico
3. Flujo de bootstrap antes
4. Cambios aplicados
5. Diff relevante
6. Flujo de bootstrap después
7. Validación de API pública
8. Validación de guards
9. Resultado de pruebas
10. Riesgos detectados
11. Recomendaciones no implementadas
12. Salida completa de terminal

CRITERIO DE ÉXITO:
- `AuthContext` ya no restaura sesión solo desde `localStorage`
- existe revalidación real contra backend al iniciar
- no hay regresiones en login/logout
- no se rompen tests ni Cypress
- el cambio queda documentado con trazabilidad completa

