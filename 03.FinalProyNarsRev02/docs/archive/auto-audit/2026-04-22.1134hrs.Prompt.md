CONTEXTO:
Estoy trabajando en un proyecto ecommerce fullstack (React + Context API + Node/Express + MongoDB).
Actualmente el carrito de compra funciona correctamente en cuanto a lógica (agregar, quitar, actualizar cantidades, stock, totales).

PROBLEMA DETECTADO:
Al presionar los botones de incrementar (+) o disminuir (-) cantidad de un producto en el carrito:
- Se muestra un mensaje de "cargando carrito"
- Toda la sección del carrito se vuelve a renderizar
- La experiencia parece como si el carrito se "recargara"

Esto ocurre en producción (Render) y también en local.

OBJETIVO:
Realizar una auditoría técnica enfocada únicamente en el comportamiento del carrito para:
1) Identificar la causa exacta del "re-render" completo o recarga visual del carrito
2) Determinar si se está haciendo un refetch innecesario del carrito completo desde backend
3) Detectar si el estado de loading está mal manejado (muy global en lugar de granular)
4) Evaluar si hay uso incorrecto de useEffect, Context o llamadas a API después de cada actualización

ALCANCE:
- Frontend React (especialmente CartPage, CartContext, hooks relacionados)
- Flujo de actualización de cantidad de productos
- Manejo de estado (loading, cart, items)
- Llamadas al backend (updateCartItem, getCart, etc.)

IMPORTANTE:
- NO modificar código todavía
- NO romper contratos entre frontend/backend
- NO hacer refactors grandes en esta etapa

FASE 1 — AUDITORÍA:
Analiza el código y responde:
- ¿Qué función se ejecuta al hacer click en + o -?
- ¿Después de actualizar cantidad se llama nuevamente a obtener todo el carrito?
- ¿Existe un loading global del carrito?
- ¿Qué dispara el mensaje "cargando carrito"?
- ¿Se está usando useEffect que vuelve a pedir el carrito tras cada cambio?
- ¿Se está re-renderizando todo el componente por cambio de estado global?

FASE 2 — DIAGNÓSTICO:
Explica claramente:
- Causa raíz del problema
- Flujo actual (paso a paso desde click hasta render)
- Qué parte provoca el “parpadeo” o recarga visual

FASE 3 — PLAN DE MEJORA (SIN EJECUTAR):
Proponer solución SIN romper nada existente:
- Evitar refetch completo del carrito en cada cambio
- Manejar loading por item (no global)
- Aplicar actualización optimista en frontend
- Mantener consistencia con backend
- Indicar exactamente qué archivos tocar y cómo

FASE 4 — VALIDACIÓN DE RIESGOS:
- ¿Hay riesgo de desincronización con backend?
- ¿Impacta pruebas E2E existentes?
- ¿Afecta contratos actuales?

FORMATO DE SALIDA:
- Crear documento en:
  C:\Users\noe.ramos\Documents\NARS\2025.Inadaptados\03.FinalProyNarsRev02\docs\archive\auto-audit\[YYYY-MM-DD_HHMM]_cart-loading-issue.md

El documento debe incluir:
- Resumen ejecutivo
- Hallazgos
- Flujo actual
- Problema identificado
- Plan propuesto (sin ejecutar)
- Riesgos

MUY IMPORTANTE:
- Incluir TODO lo que se imprima en la terminal en el documento
- No inventar nada
- Respetar contratos existentes
- No ejecutar cambios hasta aprobación explícita

fin del prompt