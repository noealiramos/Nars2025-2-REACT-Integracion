Actúa como engineer senior React especializado en state management y refactors de bajo riesgo.

OBJETIVO:
Refactorizar `ecommerce-app-Nars/src/contexts/CartContext.jsx` para usar `useReducer` en lugar de múltiples `useState`, SIN romper el contrato actual del contexto y manteniendo compatibilidad con el frontend existente, pruebas unitarias y suite Cypress.

CONTEXTO:
- El proyecto está en fase de cierre para maximizar puntuación de rúbrica.
- La auditoría vigente detectó que `Context + useReducer` está parcial porque `UIContext` usa `useReducer`, pero `CartContext` sigue con varios `useState`.
- Ya se confirmó que la suite frontend está verde (`87/87 passing`) y no se quiere introducir regresiones.
- El objetivo de este paso es recuperar el gap de rúbrica correspondiente con el menor riesgo posible.

ARCHIVO OBJETIVO:
- `ecommerce-app-Nars/src/contexts/CartContext.jsx`

REGLAS CRÍTICAS:
- SÍ debes implementar los cambios
- NO cambiar la API pública del contexto
- NO cambiar nombres de funciones expuestas
- NO cambiar comportamiento observable
- NO romper compatibilidad con Cypress ni con tests existentes
- Mantener side effects fuera del reducer
- Hacer el cambio mínimo necesario
- Si detectas que alguna mejora adicional sería útil, DOCUMENTARLA pero NO implementarla si no es indispensable para este objetivo
- Incluir TODO lo que salga en terminal dentro del documento final
- El documento final debe llevar timestamp obligatorio en formato `YYYY-MM-DD.HHmmhrs`

API PÚBLICA A PRESERVAR (VALIDAR EXPLÍCITAMENTE):
- `items`
- `cartId`
- `isLoading`
- `error`
- `isGuestMode`
- `totalItems`
- `totalPrice`
- `addItem`
- `removeItem`
- `updateQuantity`
- `clearCart`
- cualquier otra propiedad/función actualmente exportada por el contexto debe preservarse si existe

ESTRATEGIA ESPERADA:
1. Analizar el estado actual de `CartContext.jsx`
2. Identificar todos los `useState` que deben migrarse al reducer
3. Diseñar un reducer mínimo y claro, con acciones necesarias, por ejemplo:
   - `LOAD_START`
   - `LOAD_SUCCESS`
   - `LOAD_ERROR`
   - `SET_CART`
   - `CLEAR_CART`
   - `SET_GUEST_MODE`
   - usar solo las que realmente hagan falta
4. Migrar estado interno a `useReducer`
5. Adaptar las funciones existentes (`addItem`, `removeItem`, `updateQuantity`, `clearCart`, etc.) para que sigan funcionando igual
6. Validar que la API externa del contexto NO haya cambiado
7. Ejecutar validaciones técnicas
8. Documentar resultados

VALIDACIONES OBLIGATORIAS:
Ejecutar, como mínimo:
1. `npm test -- src/contexts`
2. `npm test -- src/pages`
3. `npm test`
4. Si el refactor toca comportamiento crítico del carrito, correr también:
   - `npx cypress run --spec cypress/e2e/cart.cy.js`
   - y/o la spec mínima adicional que consideres necesaria para verificar no regresión
5. Si algo falla, corregir hasta dejarlo estable o documentar exactamente por qué no quedó estable

OUTPUT OBLIGATORIO:
Crear documento en:
- `docs/auto-audit/`

Nombre:
- `YYYY-MM-DD.HHmmhrs.REFACTOR_CART_USE_REDUCER.md`

ESTRUCTURA DEL DOCUMENTO:
1. Estado inicial
2. Diagnóstico técnico
3. Diseño del reducer
4. Cambios aplicados
5. Diff relevante
6. Validación de API pública
7. Resultado de pruebas
8. Riesgos detectados
9. Recomendaciones no implementadas
10. Salida completa de terminal

CRITERIO DE ÉXITO:
- `CartContext` usa `useReducer`
- la API pública se preserva
- los tests siguen pasando
- no hay evidencia de regresión funcional
- el cambio queda documentado con trazabilidad completa