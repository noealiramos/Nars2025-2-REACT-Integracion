Actúa como engineer senior React especializado en performance, routing y refactors de bajo riesgo.

OBJETIVO:
Completar el lazy loading pendiente en `ecommerce-app-Nars/src/App.jsx` usando `React.lazy()` y `<Suspense>` para las páginas/rutas pesadas que aún se cargan eager, SIN romper rutas, guards, tests ni comportamiento observable.

CONTEXTO:
- El proyecto está en fase de cierre para maximizar puntuación de rúbrica.
- La auditoría vigente marcó `III.7 Lazy loading` como parcial porque solo algunas páginas principales usan `lazy`, mientras varias pantallas pesadas siguen importadas eager.
- Ya están verdes:
  - suite frontend
  - CartContext refactor con useReducer
  - Cypress del carrito
- Este cambio debe ser de bajo riesgo y orientado a recuperar el punto faltante de rúbrica.

ARCHIVO OBJETIVO PRINCIPAL:
- `ecommerce-app-Nars/src/App.jsx`

RUTAS/PÁGINAS A EVALUAR PARA PASAR A LAZY:
- `OrdersPage`
- `OrderDetailPage`
- `AdminProductsPage`
- `AdminCategoriesPage`
- `WishlistPage`
- `LoginPage`
- `RegisterPage`

OPCIONAL SI NO AUMENTA RIESGO:
- `CartPage`
- `ConfirmationPage`

REGLAS CRÍTICAS:
- SÍ debes implementar los cambios
- NO cambiar rutas existentes
- NO cambiar nombres de componentes
- NO cambiar guards (`PrivateRoute`, `AdminRoute`, `GuestOnlyRoute`)
- NO cambiar comportamiento observable
- Mantener compatibilidad con Vitest y Cypress
- Usar `React.lazy()` + `<Suspense>` con fallback consistente con el proyecto actual
- NO duplicar wrappers de `Suspense` innecesariamente
- NO crear componentes intermedios nuevos si no son indispensables
- Hacer el cambio mínimo necesario
- Incluir TODO lo que salga en terminal dentro del documento final
- El documento final debe llevar timestamp obligatorio en formato `YYYY-MM-DD.HHmmhrs`

VALIDACIONES OBLIGATORIAS:
Ejecutar, como mínimo:
1. `npm test -- src/pages`
2. `npm test`
3. `npm run build`
4. `npx cypress run --spec cypress/e2e/orders.cy.js`
5. Si tocaste auth routes por lazy de login/register, correr también:
   - `npx cypress run --spec cypress/e2e/auth.cy.js`

VALIDACIÓN FUNCIONAL MÍNIMA:
Confirmar explícitamente que siguen navegando correctamente, sin regresión visible:
- login
- register
- orders
- order detail
- admin products
- admin categories
- wishlist

PASOS:
1. Analizar imports actuales en `App.jsx`
2. Identificar qué páginas ya están en lazy y cuáles no
3. Documentar tabla "antes" con:
   - componente
   - tipo de import actual (`eager` o `lazy`)
   - ruta asociada
4. Convertir las páginas objetivo a `React.lazy()`
5. Verificar que todas sigan cubiertas por `Suspense`
6. Documentar tabla "después" con:
   - componente
   - tipo de import final
   - ruta asociada
7. Validar que las rutas protegidas y guest-only sigan funcionando igual
8. Ejecutar pruebas
9. Documentar cambios

OUTPUT OBLIGATORIO:
Crear documento en:
- `docs/auto-audit/`

Nombre:
- `YYYY-MM-DD.HHmmhrs.LAZY_LOADING_COMPLETION.md`

ESTRUCTURA DEL DOCUMENTO:
1. Estado inicial
2. Diagnóstico técnico
3. Tabla de imports antes
4. Cambios aplicados
5. Diff relevante
6. Tabla de imports después
7. Validación de rutas/guards
8. Resultado de pruebas
9. Riesgos detectados
10. Recomendaciones no implementadas
11. Salida completa de terminal

CRITERIO DE ÉXITO:
- Las páginas objetivo ya usan lazy loading
- `<Suspense>` sigue cubriendo correctamente
- No hay regresiones en tests
- No se rompen rutas ni guards
- El cambio queda documentado con trazabilidad completa

