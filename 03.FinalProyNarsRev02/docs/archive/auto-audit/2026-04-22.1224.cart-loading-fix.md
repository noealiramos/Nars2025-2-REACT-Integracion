# Cart loading fix

Fecha: `2026-04-22 12:24`

## Resumen ejecutivo

Se aplicó una corrección mínima y localizada en frontend para evitar que el carrito se oculte completo cuando el usuario presiona `+` o `-`.

La solución no toca `CartContext`, no cambia contratos con backend y no modifica endpoints ni payloads. El ajuste se concentró en `CartPage.jsx`, condicionando el loader global para que solo aparezca en la carga inicial real del carrito, es decir, cuando todavía no hay items visibles.

## Hallazgo confirmado

La auditoría previa quedó confirmada antes de editar código:

1. `CartContext` sigue activando `isLoading` global en `updateQuantity()`.
   Referencia: `ecommerce-app-Nars/src/contexts/CartContext.jsx:230-266`
2. `CartPage` usaba `isLoading` para mostrar `Cargando carrito...` y al mismo tiempo ocultaba lista y resumen con condiciones `!isLoading`.
   Referencia previa auditada: `docs/archive/auto-audit/2026-04-22.1206hrs.cart-loading-issue.md:7-11`, `:97-115`
3. El problema visual era de render condicional en frontend, no de un `GET /cart/user` adicional.

## Cambio aplicado

### Enfoque

Se aplicó la solución mínima pedida: tratar el loader global como loader de carga inicial real solo cuando no hay items visibles.

### Implementación

En `CartPage.jsx` se agregó:

```jsx
const isEmpty = items.length === 0;
const showInitialLoading = isLoading && isEmpty && !error && !isGuestMode;
```

Y luego:

1. el mensaje `Cargando carrito...` ahora depende de `showInitialLoading`
2. los estados de error, vacío e invitado se condicionan con `!showInitialLoading`
3. la lista de productos y el resumen ya no dependen de `!isLoading`, sino de que no exista error, no sea guest y haya items

Efecto práctico:

1. si el carrito ya tiene productos visibles y `updateQuantity()` activa `isLoading`, la UI principal se mantiene renderizada
2. no aparece el mensaje global `Cargando carrito...` durante el ajuste de cantidad
3. el flujo actual de actualización sigue igual porque no se tocó la mutación ni la integración con backend

## Archivos modificados

1. `ecommerce-app-Nars/src/pages/CartPage.jsx`
2. `ecommerce-app-Nars/src/pages/__tests__/CartPage.test.jsx`

No fue necesario tocar más de 2 archivos.

## Riesgos

Riesgo actual: bajo.

Consideraciones:

1. `isLoading` global sigue existiendo en `CartContext`; esta corrección solo evita su impacto visual más agresivo en `CartPage`
2. durante una mutación de cantidad, los botones no muestran loading granular por item; eso queda como mejora futura opcional
3. como no se cambiaron contratos ni lógica de `updateQuantity`, el riesgo principal se limita a renderización condicional de la página

## Resultado de pruebas

Se validó lo siguiente:

1. `CartPage` sigue renderizando loading inicial cuando el carrito aún no tiene items
2. `CartPage` mantiene items visibles durante `isLoading` si el carrito ya tiene productos
3. `CartContext.integration.test.jsx` siguió pasando, lo que confirma que `updateQuantity` no se rompió
4. el flujo del carrito cubierto por tests actuales sigue estable

Resultado:

1. `2` archivos de test ejecutados
2. `16` tests aprobados
3. `0` fallas

## Recomendación posterior opcional

Siguiente mejora natural, sin urgencia para este fix:

1. separar `loading` inicial de `loading` de mutación en `CartContext`
2. agregar loading granular por item para `+`, `-` y `Quitar`

No es necesario para resolver el problema visual inmediato ya corregido.

## Evidencia de código final

### `ecommerce-app-Nars/src/pages/CartPage.jsx`

Referencias relevantes tras el cambio:

1. `showInitialLoading`: `:17-18`
2. loader global inicial: `:25-27`
3. lista de items ya visible aunque `isLoading` sea `true`: `:55-74`
4. resumen ya visible aunque `isLoading` sea `true`: `:77-80`

### `ecommerce-app-Nars/src/pages/__tests__/CartPage.test.jsx`

Nueva cobertura agregada:

1. `mantiene items visibles durante loading si el carrito ya tiene productos`
   Referencia: `:35-59`

## Salida completa de terminal

### 1. Timestamp

```text
2026-04-22_1224
```

### 2. Ejecución de tests relevantes

```text
> ramdi-jewelry-ecommerce-css@1.0.0 test
> vitest run src/pages/__tests__/CartPage.test.jsx src/contexts/__tests__/CartContext.integration.test.jsx


 RUN  v4.1.1 C:/Users/noe.ramos/Documents/NARS/2025.Inadaptados/03.FinalProyNarsRev02/ecommerce-app-Nars


 Test Files  2 passed (2)
      Tests  16 passed (16)
   Start at  12:24:56
   Duration  1.73s (transform 212ms, setup 191ms, import 517ms, tests 716ms, environment 1.09s)
```
