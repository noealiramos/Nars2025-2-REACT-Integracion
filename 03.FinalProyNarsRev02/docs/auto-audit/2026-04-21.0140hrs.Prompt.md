Actúa como engineer senior frontend con enfoque en testing estable y de bajo riesgo.

OBJETIVO:
Corregir exclusivamente los 2 tests fallidos de:
`ecommerce-app-Nars/src/pages/__tests__/CheckoutPage.test.jsx`

FALLOS A CORREGIR:
- `normaliza el vencimiento numerico y continua el submit`
- `bloquea submit si el vencimiento 1/26 no cumple MM/YY`

REGLAS CRÍTICAS:
- NO modificar lógica productiva de `CheckoutPage.jsx` salvo que demuestres con evidencia que el bug es real en producción
- Priorizar arreglo del test, no del componente
- Mantener compatibilidad con la suite Cypress ya verde
- Hacer cambios mínimos y explicarlos
- Incluir TODO lo que salga en terminal en el documento final

HIPÓTESIS INICIAL A VALIDAR:
- El problema está en la fragilidad del test por uso de `user.type()` sobre un input que normaliza en `onChange`
- Posible solución:
  - usar `await user.clear(...)`
  - usar `await user.paste("12/26")` o `fireEvent.change(...)`
  - o ajustar timeout solo en esos tests
- Evitar cambios al regex o reglas del componente si Cypress y flujo real ya pasan

PLAN DE EJECUCIÓN:
1. Reproducir solo ese archivo de test
2. Confirmar la causa raíz exacta
3. Aplicar el cambio mínimo al test
4. Re-ejecutar solo ese archivo
5. Re-ejecutar toda la suite frontend
6. Documentar resultados

OUTPUT OBLIGATORIO:
Crear documento en:
`docs/auto-audit/`

Nombre:
`YYYY-MM-DD.HHmmhrs.FIX_CHECKOUT_TESTS.md`

Estructura:
1. Diagnóstico
2. Causa raíz
3. Cambio aplicado
4. Diff relevante
5. Salida completa de terminal
6. Resultado final