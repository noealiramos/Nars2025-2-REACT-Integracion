prompt de chat para antigravity:
Actúa como **Senior Backend Engineer especializado en Node.js, Express, MongoDB y seguridad de APIs REST**.

Estoy trabajando en el proyecto **ecommerce-api-Nars**, una API de e-commerce construida con:

* Node.js
* Express 5
* MongoDB / Mongoose
* JWT authentication
* Middlewares de seguridad (Helmet, CORS, Rate Limit)
* Sanitización global contra NoSQL injection
* Tests con Vitest (150 tests pasando)

La API ya está funcionando correctamente y pasó pruebas de performance.

Tu tarea es **extender el proyecto implementando tres mejoras de producción** sin romper la arquitectura actual.

---

# Objetivos a implementar

## 1️⃣ Integrar Swagger (documentación automática)

Implementar documentación de API con:

swagger-jsdoc
swagger-ui-express

Requisitos:

* Endpoint de documentación: `/api/docs`
* Documentar endpoints principales:

  * auth
  * users
  * products
  * cart
  * orders
* Incluir:

  * schemas
  * request bodies
  * responses
  * security JWT
* Usar anotaciones JSDoc dentro de las rutas.

Entregar:

* dependencias necesarias
* archivo de configuración swagger
* ejemplo de documentación en una ruta
* integración en `server.js`

---

## 2️⃣ Auditoría y mejora del sistema de logs de seguridad

Actualmente existe logging en la API.

Revisar y mejorar el sistema usando **Winston** con:

* `logs/error.log`
* `logs/combined.log`
* `logs/security.log`

Registrar automáticamente:

* errores del servidor
* intentos de login fallidos
* accesos a endpoints protegidos
* rate limit activado

Crear:

middleware `securityLogger.js`

Integrarlo en:

* login
* rutas protegidas
* errorHandler global

---

## 3️⃣ Implementar Refresh Tokens con JWT

La API actualmente usa **access tokens JWT**.

Agregar soporte para:

* refresh tokens
* renovación segura de sesión

Arquitectura requerida:

Access Token
expira en 15 minutos

Refresh Token
expira en 7 días

Endpoints nuevos:

POST `/api/auth/refresh`

Flujo:

login → accessToken + refreshToken
refresh → nuevo accessToken

Opcional:

guardar refresh tokens en MongoDB para permitir revocación.

---

# Requisitos importantes

* No romper las rutas actuales.
* Mantener compatibilidad con los tests existentes.
* Usar buenas prácticas de seguridad.
* Explicar cada archivo creado o modificado.

---

# Formato de respuesta esperado

1️⃣ Explicación general de la arquitectura
2️⃣ Archivos nuevos
3️⃣ Archivos modificados
4️⃣ Código completo listo para copiar
5️⃣ Pasos para instalar dependencias
